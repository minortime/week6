<p id="notice" class="w3-panel"><%= notice %></p>

<div class="w3-panel w3-light-blue">

<table class="w3-table-all">
<thead>
  <tr>
    <th><h1>cart</h1></th>
    <th></th>
  </tr>
</thead>
<tbody>
  
<% @cart.line_items.each do |item| %>  
<tr>
  <td><%= item.product.title %></td>
  <td><%= item.product.price %></td>
</tr>
<% end %>

<ul>
    <% @cart.line_items.each do |item| %>
    <li><%= item.quantity %> &times; <%= item.product.title %></li>
  <% end %>
</ul>
  
  
<tr>
<td>
<%= link_to 'Edit', edit_cart_path(@cart) ,class:"w3-button w3-light-gray w3-border" %> 
</td>
<td>
<%= link_to 'Back', carts_path , class:"w3-button w3-light-gray w3-border" %>
</td>
</tr>
</tbody>
</table>
</div>
<%= link_to 'Keep Shopping', store_index_url , class:"w3-button w3-blue w3-border" %>



